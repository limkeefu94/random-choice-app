{
  "name": "random-choice-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check api/gcs-signed-url.js"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.16.0"
  }
}
